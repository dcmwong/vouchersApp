/** @type {import('next').NextConfig} */
const nextConfig = {
  // Required for Cloudflare Pages deployment
  // Comment out for local `next dev`
  // experimental: { runtime: "edge" },
};

export default nextConfig;
